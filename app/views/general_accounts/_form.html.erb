<%= form_with(model: general_account) do |form| %>
  <% if general_account.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(general_account.errors.count, "error") %> prohibited this general_account from being saved:</h2>

      <ul>
        <% general_account.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :net_income, style: "display: block" %>
    <%= form.text_field :net_income %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
