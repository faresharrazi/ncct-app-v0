  <div class="row mt-2">
    
      <%= form_with(model: general_expense, local: true) do |form| %>
        <% if general_expense.errors.any? %>
          <div class="alert alert-danger">
            <h4><%= pluralize(general_expense.errors.count, "error") %> prohibited this expense from being saved:</h4>
            <ul>
              <% general_expense.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <div class="form-row align-items-center">
          <!-- Title Field -->
          <div class="col-12">
            <%= form.text_field :title, class: "form-control mb-2", placeholder: "Enter title" %>
          </div>

          <!-- Amount Field -->
          <div class="col-12">
            <div class="input-group mb-2">
              <div class="input-group-prepend">
                <div class="input-group-text">€</div>
              </div>
              <%= form.text_field :amount, class: "form-control", placeholder: "Enter amount" %>
            </div>
          </div>

          <!-- Hidden Field for General Account ID -->
          <%= form.hidden_field :general_account_id, value: 1 %>

          <!-- Submit Button -->
          <div class="col-12">
            <%= form.submit class: "btn btn-primary mb-2" %>
          </div>
        </div>
      <% end %>
  </div>
